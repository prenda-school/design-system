import React from 'react';
import { Spark, SparkProps } from './spark';

export default {
  component: Spark,
  title: 'Spark',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props: SparkProps = {};

  return <Spark />;
};
