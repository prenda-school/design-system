import { useContext } from 'react';
import SideBarContext_unstable from '../SideBarContext_unstable';

const useSideBar_unstable = () => useContext(SideBarContext_unstable);

export default useSideBar_unstable;
